usuario(__login__, senha, email)

admin(__login__, senha, email)

produto(__cod__, nome, ctipo(tipo), ccat(categoria), valor, dtfab, dtval, descr, _img_)

tipo(__cod__, descr)

categoria(__cod__, descr)
